apiVersion: apps/v1
kind: Deployment
metadata:
  name: translation-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: translation-bot
  template:
    metadata:
      labels:
        app: translation-bot
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: translation-bot
          env:
            - name: GIN_MODE
              value: release
          image: {{ .Values.image }}
      restartPolicy: Always
